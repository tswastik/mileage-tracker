
Action: file_editor create /app/frontend/src/components/MileageTrendChart.jsx --file-text "import {
  Area,
  AreaChart,
  CartesianGrid,
  ResponsiveContainer,
  Tooltip,
  XAxis,
  YAxis,
} from \"recharts\";
import { monthLabel, formatNum } from \"@/lib/format\";

const CustomTooltip = ({ active, payload, label }) => {
  if (!active || !payload || !payload.length) return null;
  return (
    <div className=\"bg-white border border-[#E5E3DB] rounded-sm px-3 py-2 text-sm\">
      <div className=\"text-[#6B6A65] text-xs uppercase tracking-wider mb-1\">
        {monthLabel(label)}
      </div>
      <div className=\"text-[#2C2C2A] font-medium\">
        {formatNum(payload[0].value, \" km/L\", 2)}
      </div>
    </div>
  );
};

export default function MileageTrendChart({ data }) {
  const chartData = (data || []).map((d) => ({
    month: d.month,
    mileage: d.avg_mileage_kmpl,
  }));

  if (chartData.filter((d) => d.mileage != null).length === 0) {
    return (
      <div
        data-testid=\"mileage-trend-empty\"
        className=\"h-64 flex items-center justify-center text-sm text-[#6B6A65] border border-dashed border-[#E5E3DB] rounded-sm\"
      >
        Add at least 2 refuels to see your mileage trend.
      </div>
    );
  }

  return (
    <div data-testid=\"mileage-trend-chart\" style={{ width: \"100%\", height: 280 }}>
      <ResponsiveContainer>
        <AreaChart data={chartData} margin={{ top: 8, right: 16, left: -10, bottom: 0 }}>
          <defs>
            <linearGradient id=\"mileageFill\" x1=\"0\" y1=\"0\" x2=\"0\" y2=\"1\">
              <stop offset=\"0%\" stopColor=\"#2E5339\" stopOpacity={0.35} />
              <stop offset=\"100%\" stopColor=\"#2E5339\" stopOpacity={0.02} />
            </linearGradient>
          </defs>
          <CartesianGrid stroke=\"#E5E3DB\" vertical={false} strokeDasharray=\"0\" />
          <XAxis
            dataKey=\"month\"
            tickFormatter={monthLabel}
            stroke=\"#6B6A65\"
            tick={{ fontSize: 12 }}
            axisLine={{ stroke: \"#E5E3DB\" }}
            tickLine={false}
          />
          <YAxis
            stroke=\"#6B6A65\"
            tick={{ fontSize: 12 }}
            axisLine={false}
            tickLine={false}
            width={48}
          />
          <Tooltip content={<CustomTooltip />} cursor={{ stroke: \"#2E5339\", strokeDasharray: 3 }} />
          <Area
            type=\"monotone\"
            dataKey=\"mileage\"
            stroke=\"#2E5339\"
            strokeWidth={2}
            fill=\"url(#mileageFill)\"
            connectNulls
          />
        </AreaChart>
      </ResponsiveContainer>
    </div>
  );
}
"
Observation: Create successful: /app/frontend/src/components/MileageTrendChart.jsx